import React from 'react';
import { FormComponent } from '../../components/FormComponent';

type Props = {};

function page({}: Props) {
  return (
    <div>
      <FormComponent />
    </div>
  );
}

export default page;
